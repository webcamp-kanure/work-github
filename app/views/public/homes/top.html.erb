<div class="container">
  <div class="row py-4">

    <div class="col-md-3">
      <div class="row">

        <div class="navbar-collapse border border-3 border-dark mr-5 mt-2 "style="height: 400px;">
          <ul class="navbar-nav nav-stacked">
            <li class="nav-item border-bottom border-dark text-center" style="width: 100%;">ジャンル検索</li>
            <% @genres.each do |genre| %>
              <li class="nav-item my-2 ml-4"><%= genre.name %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <div class="col-md-9">
      <!--仮の画像を挿入中-->
      <div class="row my-2 center-block" style="width: 80%; height: 60%; background-image: url('<%= asset_path('cloth.jpg') %>'); background-size: cover; background-position: center;">

        <div class="col-md-10 offset-md-1 d-flex align-items-center justify-content-center">
          <div class="row m-5 bg-light border border-3 border-dark">
            <div class="text-left py-4">
              ようこそ、ながのCAKEへ!<br>
              このサイトは、ケーキ販売のECサイトになります。<br>
              会員でない方も商品の閲覧はできますが、<br>
              購入には会員登録が必要になります。
            </div>
          </div>
        </div>
      </div>

      <div class="row my-2">
        <h1>新着商品</h1>
      </div>
      <div class="row my-2">
        <% @items.first(4).each do |item| %>
          <div class="col-3 p-1">
            <%= link_to item_path(item.id) do %>
              <%= image_tag item.get_image, class: "img-fluid", width: 300, height: 300 %></br>
            <% end %>
            <%= link_to item.name, item_path(item.id) %></br>
            ¥<%= p item.price.to_s(:delimited) %>
          </div>
        <% end %>
      </div>

      <div class="text-right">
        <%= link_to "全ての商品を見る>", items_path %>
      </div>

    </div>

  </div>
</div>
